This directory archives legacy API files that have been migrated into the modular `server/` structure.

Files:

- `api.index.js` — original monolithic API. Kept for reference; not loaded by the server.

Remove this folder only when you're certain the new `server/` routes fully replace all clients.
